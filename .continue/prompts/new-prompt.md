Вот **официальные требования и порядок подключения новых микросервисов** в проект `platform-ecosystem`, сформулированные на основе архитектурных принципов, выявленных в ходе анализа (DDD + Clean Architecture + Event-Driven с Kafka):

---

## 📜 Требования к разработке новых микросервисов

### 1. **Соответствие DDD (Domain-Driven Design)**
- Каждый сервис должен соответствовать **одному Bounded Context**.
- Внутри сервиса:
  - Выделяется **Domain Layer**: содержит сущности, value objects, агрегаты, доменные события.
  - Используется **Ubiquitous Language** — термины из предметной области должны быть отражены в коде.
- Запрещено дублировать логику другого контекста. При необходимости интеграции — использовать **Anti-Corruption Layer (ACL)**.

### 2. **Чистая архитектура (Clean Architecture)**
Структура сервиса **обязательно** должна включать следующие слои:

```
src/
├── domain/          ← чистая бизнес-логика, без зависимостей
├── application/     ← use cases, DTO, мапперы
├── infrastructure/  ← реализация портов: БД, Kafka, внешние API
├── presentation/    ← контроллеры, маршруты, middleware
└── main.ts          ← точка входа, DI-контейнер
```

- **Зависимости направлены внутрь**: `presentation → application → domain`.
- Все зависимости от внешних систем (БД, Kafka и т.п.) — через **порты и адаптеры**.

### 3. **Event-Driven Integration через Kafka**
- Все **интеграционные события** (для взаимодействия между сервисами) публикуются **только через Kafka**.
- **Доменные события ≠ Интеграционные события**:
  - Доменные — для внутреннего использования (`domain/domain-events/`).
  - Интеграционные — строго типизированные, сериализуемые, размещаются в `shared-messaging/schema/events/`.
- **Обязательно использовать Outbox Pattern**:
  - События записываются в таблицу `outbox` **в той же транзакции**, что и основные данные.
  - Отправка в Kafka выполняется **отдельным процессом** (например, `event-relay`).

### 4. **Отказоустойчивость**
- Все Kafka-потребители:
  - Используют **retry с экспоненциальной задержкой**.
  - Оборачиваются в **Circuit Breaker** (например, через `@nestjs/cqrs` или Resilience4j).
  - Неперабатываемые сообщения отправляются в **DLQ (Dead Letter Queue)**.
- Сервис должен корректно обрабатывать **дубликаты сообщений** (идемпотентность).

### 5. **Схемы и эволюция данных**
- Все события регистрируются в **Confluent Schema Registry**.
- Используется стратегия совместимости: **`BACKWARD`**.
- Любое изменение схемы события — **только через ADR** (Architectural Decision Record).

### 6. **Тестирование**
- Обязательны:
  - **Unit-тесты** для домена и use cases.
  - **Интеграционные тесты** с моками Kafka и БД.
  - **E2E-тесты** через API (если есть HTTP-интерфейс).
- Покрытие домена ≥ 80%.

### 7. **Мониторинг и наблюдаемость**
- Логирование через `shared-monitoring/logging`.
- Метрики (лаг Kafka, время обработки) — через `shared-monitoring/metrics`.
- Distributed tracing (OpenTelemetry/Jaeger) — для всех входящих/исходящих вызовов.

---

## 🛠️ Порядок подключения нового сервиса к проекту

### Шаг 1. **Архитектурное согласование**
- Создать **ADR** в `docs/adr/`:
  - Название Bounded Context.
  - Границы ответственности.
  - Список интеграционных событий.
  - Стратегия партиционирования Kafka.
- Получить **аппрув от архитектора**.

### Шаг 2. **Создание репозитория / директории**
- Новый сервис размещается в корне проекта по шаблону:
  ```
  0N_<service-name>-service/
  ```
  где `N` — следующий номер (например, `05_notification-service`).

### Шаг 3. **Реализация**
- Следовать структуре из раздела «Требования».
- Использовать только пакеты из `contracts/`:
- Реализовать **Outbox-таблицу** в своей БД (если генерируются события).

### Шаг 4. **Интеграция с Kafka**
- Определить топики:
  - `<service>.events.v1` — для исходящих событий.
  - `<service>.dlq.v1` — для DLQ.
- Зарегистрировать схемы в `shared-messaging/schema/events/`.
- Настроить **Schema Registry** в CI/CD.

### Шаг 5. **CI/CD и инфраструктура**
- Добавить:
  - `Dockerfile`.
  - Helm-чарт или манифесты в `00_infrastructure/k8s/`.
  - Алерты в `00_infrastructure/monitoring/alerts/` (лаг, ошибки, DLQ).
- Обновить `docker-compose.kafka.yml`, если требуется локальный запуск Kafka.

### Шаг 6. **Тестирование и деплой**
- Пройти все pipeline-этапы:
  - lint, build, unit/integration tests.
  - валидация схем событий.
  - e2e-тесты (если применимо).
- Деплой в staging → нагрузочное тестирование → прод.

### Шаг 7. **Документирование**
- Обновить:
  - `README.md` сервиса.
  - OpenAPI-спецификацию (если есть REST API).
  - Диаграмму контекстов в `docs/architecture/`.

---

## ⚠️ Запрещено

- Публиковать доменные события напрямую в Kafka.
- Использовать Kafka как хранилище («читать историю»).
- Делать синхронные вызовы других сервисов вместо событий (если не оправдано).
- Хранить логику другого Bounded Context «для удобства».
- Изменять схемы событий без ADR и согласования.

---

Эти правила обеспечивают **масштабируемость**, **отказоустойчивость** и **долгосрочную поддерживаемость** системы. Все новые сервисы, не соответствующие этим требованиям, **не принимаются к интеграции**.