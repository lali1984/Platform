# User Service

Микросервис управления пользователями в экосистеме platform-ecosystem.

## 📋 Обзор

User Service отвечает за управление пользователями (CRUD операции), их профилями и статусами. Сервис реализован в соответствии с принципами DDD (Domain-Driven Design) и Clean Architecture, использует Event-Driven подход для интеграции с другими сервисами через Kafka.

### Bounded Context
- **Управление пользователями**: создание, чтение, обновление, удаление пользователей
- **Управление профилями**: обновление персональных данных, аватаров
- **Управление статусами**: активация, деактивация, верификация пользователей

## 🏗️ Архитектура

### Структура проекта
```
04_user-service/
├── src/
│   ├── domain/                    # Доменный слой (чистая бизнес-логика)
│   │   ├── entities/              # Сущности (User)
│   │   ├── value-objects/         # Value Objects (Email, Phone)
│   │   ├── domain-events/         # Доменные события
│   │   ├── ports/                 # Порт-интерфейсы
│   │   └── base/                  # Базовые классы DDD
│   ├── application/               # Слой приложения
│   │   ├── use-cases/             # Use Cases
│   │   ├── dto/                   # Data Transfer Objects
│   │   └── mappers/               # Мапперы
│   ├── infrastructure/            # Инфраструктурный слой
│   │   ├── persistence/           # Репозитории, сущности БД
│   │   ├── messaging/             # Kafka producer/consumer
│   │   ├── config/                # Конфигурации
│   │   └── security/              # Безопасность
│   └── presentation/              # Слой представления
│       ├── controllers/           # Контроллеры REST API
│       ├── dto/                   # DTO для API
│       └── middleware/            # Middleware
├── tests/                         # Тесты
├── api/                           # OpenAPI спецификация
└── Dockerfile                     # Docker конфигурация
```

### Технологический стек
- **Runtime**: Node.js 18+
- **Framework**: NestJS
- **База данных**: PostgreSQL
- **Message Broker**: Apache Kafka
- **ORM**: TypeORM
- **Документация**: Swagger/OpenAPI
- **Тестирование**: Jest

## 🚀 Быстрый старт

### Предварительные требования
- Docker и Docker Compose
- Node.js 18+
- PostgreSQL 14+
- Apache Kafka (через docker-compose)

### Установка и запуск

1. **Клонирование и настройка**
```bash
# Клонирование репозитория
git clone <repository-url>
cd platform-ecosystem

# Установка зависимостей
cd 04_user-service
npm install
```

2. **Настройка окружения**
```bash
# Копирование файла окружения
cp .env.example .env.local

# Редактирование .env.local (укажите свои значения)
DATABASE_HOST=postgres-user
DATABASE_PORT=5432
DATABASE_USERNAME=admin
DATABASE_PASSWORD=secret
DATABASE_NAME=user_db
KAFKA_BROKERS=localhost:9092
PORT=3002
```

3. **Запуск через Docker Compose**
```bash
# Запуск всей инфраструктуры
cd ..
docker-compose up -d postgres-user kafka zookeeper

# Запуск сервиса
cd 04_user-service
npm run start:dev
```

4. **Миграции базы данных**
```bash
# Генерация миграции
npm run migration:generate -- -n InitialMigration

# Применение миграций
npm run migration:run
```

## 📡 API Endpoints

### Основные endpoints
- `GET /api/v1/users` - Получить список пользователей
- `GET /api/v1/users/:id` - Получить пользователя по ID
- `POST /api/v1/users` - Создать нового пользователя
- `PUT /api/v1/users/:id` - Обновить пользователя
- `DELETE /api/v1/users/:id` - Удалить пользователя
- `PATCH /api/v1/users/:id/verify` - Верифицировать пользователя
- `PATCH /api/v1/users/:id/deactivate` - Деактивировать пользователя

### Health check
- `GET /api/v1/health` - Проверка здоровья сервиса

### Документация API
- `GET /api/docs` - Swagger документация

## 🔧 Конфигурация

### Переменные окружения
| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `PORT` | Порт сервиса | `3002` |
| `DATABASE_HOST` | Хост PostgreSQL | `postgres-user` |
| `DATABASE_PORT` | Порт PostgreSQL | `5432` |
| `DATABASE_USERNAME` | Имя пользователя БД | `admin` |
| `DATABASE_PASSWORD` | Пароль БД | `secret` |
| `DATABASE_NAME` | Имя базы данных | `user_db` |
| `KAFKA_BROKERS` | Kafka brokers | `localhost:9092` |
| `NODE_ENV` | Окружение | `development` |
| `CORS_ORIGIN` | CORS origins | `*` |

### Конфигурация Kafka
Сервис публикует события в следующие топики:
- `user.events.v1` - События пользователей (создание, обновление, удаление)
- `auth.events.v1` - События аутентификации (регистрация, логин)

## 🎯 Use Cases

### Создание пользователя
1. Валидация email и телефона
2. Проверка уникальности email
3. Создание доменного объекта User
4. Сохранение в БД
5. Публикация события `user.created` в Kafka

### Обновление профиля
1. Валидация обновляемых полей
2. Обновление доменного объекта
3. Сохранение изменений
4. Публикация события `user.updated` в Kafka

### Удаление пользователя
1. Мягкое удаление (soft delete)
2. Изменение статуса на `DELETED`
3. Публикация события `user.deleted` в Kafka

## 📊 Модель данных

### Сущность User
```typescript
interface User {
  id: string;                    // UUID
  email: Email;                  // Value Object
  firstName: string;
  lastName: string;
  phone?: Phone;                 // Value Object
  avatarUrl?: string;
  status: UserStatus;            // ACTIVE, INACTIVE, SUSPENDED, DELETED
  isVerified: boolean;
  metadata: Record<string, any>;
  createdAt: Date;
  updatedAt: Date;
  deletedAt?: Date;
}
```

### Value Objects
- **Email**: Валидация формата, нормализация
- **Phone**: Валидация формата, нормализация

## 🔌 Интеграция с другими сервисами

### Публикуемые события
| Событие | Топик | Описание |
|---------|-------|----------|
| `user.created` | `user.events.v1` | Создан новый пользователь |
| `user.updated` | `user.events.v1` | Обновлен профиль пользователя |
| `user.deleted` | `user.events.v1` | Пользователь удален |
| `user.registered` | `auth.events.v1` | Пользователь зарегистрирован |
| `user.verified` | `user.events.v1` | Пользователь верифицирован |

### Подписываемые события
Сервис подписывается на:
- `auth.events.v1` - События аутентификации
- `notification.events.v1` - События уведомлений

## 🧪 Тестирование

### Запуск тестов
```bash
# Unit тесты
npm test

# E2E тесты
npm run test:e2e

# Тесты с покрытием
npm run test:cov
```

### Структура тестов
```
tests/
├── unit/           # Unit тесты домена и use cases
├── integration/    # Интеграционные тесты
└── e2e/           # End-to-end тесты API
```

## 🐳 Docker

### Сборка образа
```bash
docker build -t user-service:latest .
```

### Запуск контейнера
```bash
docker run -p 3002:3002 \
  -e DATABASE_HOST=postgres \
  -e DATABASE_PORT=5432 \
  -e KAFKA_BROKERS=kafka:9092 \
  user-service:latest
```

## 📈 Мониторинг

### Health checks
- `GET /api/v1/health` - Проверка подключения к БД и Kafka
- `GET /api/v1/health/liveness` - Liveness probe
- `GET /api/v1/health/readiness` - Readiness probe

### Метрики
- Количество пользователей
- Время ответа API
- Ошибки Kafka producer
- Статус подключения к БД

### Логирование
- Структурированные логи в формате JSON
- Уровни: error, warn, info, debug
- Correlation ID для трассировки запросов

## 🔒 Безопасность

### Аутентификация
- JWT токены через auth-service
- Валидация токенов через shared-security

### Авторизация
- RBAC (Role-Based Access Control)
- Проверка прав доступа к операциям

### Валидация данных
- Class-validator для DTO
- Кастомные валидаторы для Value Objects
- SQL инъекции предотвращаются через TypeORM

## 🔄 Миграции

### Создание миграции
```bash
npm run migration:generate -- -n MigrationName
```

### Применение миграций
```bash
npm run migration:run
```

### Откат миграции
```bash
npm run migration:revert
```

## 🚨 Обработка ошибок

### Типы ошибок
- `ValidationError` - Ошибки валидации данных
- `NotFoundError` - Ресурс не найден
- `ConflictError` - Конфликт данных (дубликаты)
- `DatabaseError` - Ошибки базы данных
- `KafkaError` - Ошибки Kafka producer

### Стратегия retry
- Экспоненциальная задержка для Kafka producer
- Circuit breaker для внешних зависимостей
- Dead Letter Queue для неперабатываемых сообщений

## 📚 Дополнительная документация

### Ссылки
- [Архитектура проекта](../docs/architecture/README.md)
- [DDD принципы](../docs/ddd/README.md)
- [Event-Driven архитектура](../docs/event-driven/README.md)
- [Kafka интеграция](../docs/kafka/README.md)

### ADR (Architectural Decision Records)
- [ADR-001: Выбор архитектуры DDD + Clean Architecture](../docs/adr/001-ddd-clean-architecture.md)
- [ADR-002: Event-Driven интеграция через Kafka](../docs/adr/002-event-driven-kafka.md)
- [ADR-003: Модель данных пользователя](../docs/adr/003-user-data-model.md)

## 🤝 Вклад в разработку

### Code style
- TypeScript с strict mode
- ESLint + Prettier
- Комментарии JSDoc для публичных методов

### Git workflow
- Feature branches
- Pull requests с code review
- Semantic versioning

### Тестирование
- Минимальное покрытие 80% для доменного слоя
- Интеграционные тесты для use cases
- E2E тесты для API endpoints

## 📞 Поддержка

### Команда разработки
- Lead Developer: [Имя]
- Backend Team: [Контакты]

### Мониторинг и алерты
- Grafana dashboard: [Ссылка]
- Alert manager: [Конфигурация]
- Incident response: [Процедура]

---

*Последнее обновление: $(date)*