{
  "name": "platform-ecosystem",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "build": "pnpm --filter \"./contracts\" run build && pnpm --filter \"./03_auth-service\" run build",
    "build:contracts": "pnpm --filter \"./contracts\" run build",
    "build:auth": "pnpm --filter \"./03_auth-service\" run build",
    "clean": "rm -rf node_modules */node_modules"
  }
}