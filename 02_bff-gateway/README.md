Теперь создам полную документацию на основе проведенного анализа:

# 📋 Документация BFF Gateway Service

## 🏗️ Архитектурный обзор

**BFF Gateway** — это Backend for Frontend сервис, построенный по принципам **Clean Architecture** и **Domain-Driven Design (DDD)**. Сервис выступает единой точкой входа для фронтенд-приложений, агрегируя данные из микросервисов и предоставляя оптимизированные API.

### 📊 Архитектурные слои

```
src/
├── domain/           # Чистая бизнес-логика (DDD)
│   ├── aggregates/   # Агрегаты DDD
│   ├── ports/        # Интерфейсы (порты)
│   └── value-objects/# Value Objects
├── application/      # Use Cases
│   ├── use-cases/    # Бизнес-сценарии
│   ├── dto/          # Data Transfer Objects
│   └── mappers/      # Преобразователи данных
├── infrastructure/   # Внешние зависимости
│   ├── config/       # Конфигурация
│   ├── http-clients/ # HTTP клиенты к микросервисам
│   └── cache/        # Кэширование
├── presentation/     # API слой
│   ├── controllers/  # Контроллеры
│   ├── middleware/   # Middleware
│   └── routes/       # Маршруты
└── shared/          # Общие утилиты
```

## 🎯 Bounded Context

**BFF Gateway** отвечает за:
- **Агрегацию данных** из микросервисов
- **Аутентификацию и авторизацию** пользователей
- **Оптимизацию запросов** для фронтенда
- **Кэширование** часто запрашиваемых данных
- **Трансформацию данных** под нужды UI

## 🔧 Технологический стек

- **Node.js 20** с TypeScript 5.2
- **Express.js** для HTTP сервера
- **Axios** для HTTP клиентов
- **In-memory cache** (с возможностью Redis)
- **JWT** для аутентификации
- **Helmet, CORS, Compression** для безопасности и производительности

## 📁 Структура проекта

### 1. Domain Layer (Чистая бизнес-логика)

#### Агрегаты (Aggregates)
- **UserProfileAggregate** — агрегат профиля пользователя, объединяющий данные из auth и user сервисов
  ```typescript
  class UserProfileAggregate {
    constructor(
      public readonly authData: UserAuthData,
      public readonly profileData: CompleteUserProfile,
      public readonly metadata: { ... }
    )
  }
  ```

#### Порты (Ports)
- **IAuthClient** — интерфейс для работы с auth-сервисом
- **IUserClient** — интерфейс для работы с user-сервисом  
- **ICache** — интерфейс для кэширования

#### Value Objects
- **ApiResponse** — стандартизированный ответ API
- **PaginatedResponse** — пагинированный ответ

### 2. Application Layer (Use Cases)

#### Use Cases
- **GetUserProfileUseCase** — получение профиля пользователя с кэшированием
- **AuthUserUseCase** — аутентификация и регистрация пользователей

### 3. Infrastructure Layer (Внешние зависимости)

#### HTTP Clients
- **AuthHttpClient** — клиент для auth-сервиса
- **UserHttpClient** — клиент для user-сервиса
- **HttpClientFactory** — фабрика HTTP клиентов с настройками

#### Конфигурация
- **environment.ts** — переменные окружения
- **bff.config.ts** — конфигурация сервиса

#### Кэширование
- **InMemoryCacheAdapter** — адаптер in-memory кэша

### 4. Presentation Layer (API)

#### Контроллеры
- **UserController** — управление пользователями
- **AuthController** — аутентификация

#### Middleware
- **AuthMiddleware** — проверка JWT токенов
- **ErrorHandlerMiddleware** — обработка ошибок

#### Маршруты
- **ApiRoutes** — определение всех API эндпоинтов

## 🔌 Интеграция с микросервисами

### Auth Service
- **URL**: `AUTH_SERVICE_URL` (по умолчанию: `http://localhost:3001`)
- **Эндпоинты**: `/auth/validate`, `/auth/login`, `/auth/register`

### User Service  
- **URL**: `USER_SERVICE_URL` (по умолчанию: `http://localhost:3002`)
- **Эндпоинты**: `/users/profile`, `/users/preferences`

## 🚀 Запуск и развертывание

### Локальный запуск
```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка
npm run build

# Запуск production версии
npm start
```

### Docker
```bash
# Сборка образа
docker build -t bff-gateway .

# Запуск контейнера
docker run -p 3003:3003 \
  -e NODE_ENV=production \
  -e AUTH_SERVICE_URL=http://auth-service:3001 \
  -e USER_SERVICE_URL=http://user-service:3002 \
  bff-gateway
```

### Переменные окружения
```env
NODE_ENV=development|production|test
PORT=3003
AUTH_SERVICE_URL=http://localhost:3001
USER_SERVICE_URL=http://localhost:3002
REDIS_URL=redis://localhost:6379  # опционально
JWT_SECRET=your-secret-key
CACHE_TTL=300  # время жизни кэша в секундах
```

## 📊 API Endpoints

### Публичные эндпоинты

#### GET `/`
Информация о сервисе
```json
{
  "service": "BFF Gateway",
  "version": "1.0.0",
  "architecture": "Clean Architecture + DDD",
  "environment": "development",
  "endpoints": { ... }
}
```

#### GET `/health`
Health check
```json
{
  "status": "ok",
  "service": "bff-gateway",
  "timestamp": "2024-01-15T10:30:00Z"
}
```

#### GET `/api/test`
Тестовый эндпоинт
```json
{
  "message": "BFF Gateway is working with Clean Architecture",
  "architecture": "Clean Architecture + DDD",
  "timestamp": "2024-01-15T10:30:00Z"
}
```

### Аутентификация

#### POST `/api/auth/register`
Регистрация пользователя
```json
{
  "email": "user@example.com",
  "password": "securepassword",
  "username": "johndoe",
  "firstName": "John",
  "lastName": "Doe"
}
```

#### POST `/api/auth/login`
Вход в систему
```json
{
  "email": "user@example.com",
  "password": "securepassword"
}
```

#### POST `/api/auth/logout`
Выход из системы

#### POST `/api/auth/refresh`
Обновление токена
```json
{
  "refreshToken": "refresh_token_here"
}
```

### Пользователи (требуют аутентификации)

#### GET `/api/users/profile/me`
Получение профиля текущего пользователя
```json
{
  "success": true,
  "data": {
    "id": "user_123",
    "email": "user@example.com",
    "username": "johndoe",
    "firstName": "John",
    "lastName": "Doe",
    "avatarUrl": "https://example.com/avatar.jpg",
    "bio": "Software developer",
    "permissions": ["read:profile", "write:profile"],
    "lastLogin": "2024-01-15T10:30:00Z"
  }
}
```

#### GET `/api/users/profile/:userId`
Получение профиля другого пользователя

#### GET `/api/users/search`
Поиск пользователей
```json
{
  "query": "john",
  "limit": 10
}
```

### Административные эндпоинты

#### GET `/api/admin/dashboard`
Административная панель (требует роль `admin`)

## 🔐 Аутентификация и авторизация

### JWT токены
- Используется Bearer token авторизация
- Токены валидируются через auth-сервис
- Поддерживаются роли и разрешения

### Middleware
- **authenticate** — обязательная аутентификация
- **optionalAuth** — опциональная аутентификация
- **requireRole** — проверка роли
- **requirePermission** — проверка разрешения

## 💾 Кэширование

### Стратегия кэширования
- **In-memory кэш** по умолчанию
- **Redis** для production окружения
- **TTL**: 5 минут (настраивается через `CACHE_TTL`)
- **Ключи кэша**: `user_profile_<userId>`, `user:<userId>:profile`

### Преимущества
- Уменьшение нагрузки на микросервисы
- Ускорение ответов API
- Автоматическая инвалидация по TTL

## 🛡️ Безопасность

### Защита
- **Helmet** — защита HTTP заголовков
- **CORS** — контроль доступа
- **Rate limiting** — ограничение запросов
- **Request timeout** — 10 секунд

### Валидация
- Валидация JWT токенов
- Проверка ролей и разрешений
- Валидация входных данных

## 📈 Мониторинг и логирование

### Логирование
- Структурированные логи
- Уровни логирования по окружениям
- Логирование ошибок и важных событий

### Метрики
- Время ответа API
- Статус коды ответов
- Использование кэша
- Ошибки интеграции с микросервисами

## 🔄 Паттерны проектирования

### Clean Architecture
- Слоистая архитектура
- Инверсия зависимостей
- Независимость от фреймворков

### DDD (Domain-Driven Design)
- Агрегаты (UserProfileAggregate)
- Порты и адаптеры
- Ubiquitous Language

### CQRS (Command Query Responsibility Segregation)
- Разделение чтения и записи
- Оптимизированные запросы для чтения

### Circuit Breaker
- Защита от сбоев микросервисов
- Экспоненциальная задержка при повторах

## 🧪 Тестирование

### Unit тесты
- Домен и use cases
- Value objects
- Агрегаты

### Интеграционные тесты
- HTTP клиенты
- Middleware
- Контроллеры

### E2E тесты
- Полный цикл API
- Интеграция с микросервисами

## 🔄 Жизненный цикл запроса

1. **Запрос** → Express middleware (CORS, Helmet, Compression)
2. **Аутентификация** → AuthMiddleware → AuthHttpClient → Auth Service
3. **Бизнес-логика** → Controller → Use Case → Domain
4. **Кэширование** → Check cache → Update cache
5. **Интеграция** → HTTP клиенты → Микросервисы
6. **Ответ** → Стандартизированный ApiResponse

## 📚 Дополнительная документация

### Архитектурные решения
- [ADR] Выбор Clean Architecture для BFF
- [ADR] Использование DDD агрегатов для профиля пользователя
- [ADR] Стратегия кэширования в BFF

### Руководства разработчика
- Добавление нового use case
- Интеграция с новым микросервисом
- Настройка middleware

### Операционная документация
- Мониторинг и алерты
- Масштабирование
- Резервное копирование и восстановление

---

## 🎯 Ключевые преимущества архитектуры

1. **Масштабируемость** — независимые слои, легко добавлять новые use cases
2. **Поддерживаемость** — чистый код, разделение ответственности
3. **Тестируемость** — изолированные компоненты, моки зависимостей
4. **Гибкость** — легко заменять реализации (кэш, HTTP клиенты)
5. **Надежность** — обработка ошибок, retry логика, circuit breaker

## 🔮 Планы развития

1. **Redis кэширование** — для production окружений
2. **GraphQL API** — для гибких запросов от фронтенда
3. **WebSocket поддержка** — для real-time обновлений
4. **Rate limiting** — защита от DDoS атак
5. **OpenAPI документация** — автоматическая генерация спецификаций

---

*Документация обновлена: 2024-01-15*
*Версия сервиса: 1.0.0*
*Архитектура: Clean Architecture + DDD*