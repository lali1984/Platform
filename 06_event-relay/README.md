# 🚀 Event Relay Service

**Event Relay** — это микросервис для ретрансляции событий из outbox таблиц в Kafka. Он реализует паттерн [Transactional Outbox](https://microservices.io/patterns/data/transactional-outbox.html), обеспечивая надежную доставку событий между микросервисами.

---

## 🏗️ Архитектурная схема

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   │ Auth  │   │    │   │ User  │   │    │   │ Other │   │
│   │ Service │   │    │   │ Service │   │    │   │ Service │   │
└────────┬────────┘    └────────┬────────┘    └────────┬────────┘
         │                      │                      │
  ┌──────▼──────┐        ┌──────▼──────┐        ┌──────▼──────┐
  │    Outbox   │        │    Outbox   │        │    Outbox   │
  │   Table     │        │   Table     │        │   Table     │
  └────────┬────┘        └────────┬────┘        └────────┬────┘
           │                    │                      │
           └──────────────────┼──────────────────────┘
                                │
                         ┌──────▼──────┐
                         │ Event Relay │
                         │   Service   │
                         ┌──────▼──────┐
                         │    Kafka    │
```

---

## 🔧 Основные функции

1. **Polling outbox таблиц** всех микросервисов каждые 5 секунд
2. **Чтение pending событий** из outbox таблиц
3. **Публикация событий** в соответствующие Kafka топики
4. **Обработка ошибок** с retry-механизмом и Circuit Breaker
5. **Отправка в DLQ** необработанных событий после 3 попыток
6. **Очистка старых опубликованных событий**

---

## 📁 Структура проекта

```
06_event-relay/
├── src/
│   ├── application/               # Бизнес-логика
│   │   └── EventRelayApplication.ts
│   ├── domain/                    # Доменные объекты (пусто - используем shared-domain)
│   ├── infrastructure/            # Внешние зависимости
│   │   ├── db/                    # Работа с БД
│   │   │   ├── DatabaseConfig.ts
│   │   │   └── OutboxReaderRepository.ts
│   │   └── messaging/            # Работа с Kafka
│   │       ├── KafkaProducerService.ts
│   │       └── CircuitBreaker.ts
│   ├── presentation/             # API и health checks
│   │   └── HealthController.ts
│   └── main.ts                   # Точка входа
├── .env.example                  # Пример переменных окружения
├── Dockerfile                   # Docker конфигурация
├── package.json                 # Зависимости и скрипты
└── tsconfig.json                # TypeScript конфигурация
```

---

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно | По умолчанию |
|------------|----------|-------------|--------------|
| `PORT` | Порт сервиса | Нет | `3006` |
| `NODE_ENV` | Окружение | Нет | `development` |
| `KAFKA_BROKERS` | Адреса Kafka брокеров | Да | - |
| `KAFKA_CLIENT_ID` | Client ID для Kafka | Нет | `event-relay` |
| `AUTH_DB_HOST` | Хост БД auth-service | Да | - |
| `AUTH_DB_PORT` | Порт БД auth-service | Нет | `5432` |
| `AUTH_DB_USERNAME` | Пользователь БД auth | Да | - |
| `AUTH_DB_PASSWORD` | Пароль БД auth | Да | - |
| `AUTH_DB_DATABASE` | Имя БД auth | Да | `auth_db` |
| `USER_DB_HOST` | Хост БД user-service | Да | - |
| `USER_DB_PORT` | Порт БД user-service | Нет | `5432` |
| `USER_DB_USERNAME` | Пользователь БД user | Да | - |
| `USER_DB_PASSWORD` | Пароль БД user | Да | - |
| `USER_DB_DATABASE` | Имя БД user | Да | `user_db` |
| `POLLING_INTERVAL_MS` | Интервал опроса outbox (ms) | Нет | `5000` |
| `MAX_ATTEMPTS_PER_EVENT` | Макс. попыток отправки | Нет | `3` |

### Конфигурация Circuit Breaker

```typescript
{
  failureThreshold: 5,           // Количество ошибок до перехода в OPEN
  resetTimeout: 60000,          // Время в ms перед переходом в HALF_OPEN
  halfOpenMaxAttempts: 3         // Макс. попыток в HALF_OPEN
}
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd 06_event-relay
npm install
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env файл при необходимости
```

### 3. Запуск в development режиме

```bash
npm run dev
```

### 4. Сборка и запуск

```bash
npm run build
npm start
```

### 5. Запуск через Docker

```bash
docker-compose up event-relay
```

---

## 📡 API Endpoints

### Health Check

```http
GET /health
```

**Response:**
```json
{
  "status": "healthy",
  "timestamp": "2024-01-21T20:06:00.000Z",
  "services": {
    "auth-service": "connected",
    "user-service": "connected"
  },
  "kafka": {
    "isConnected": true,
    "circuitBreakerState": "CLOSED"
  }
}
```

### Статус сервиса

```http
GET /status
```

**Response:**
```json
{
  "isRunning": true,
  "monitoredServices": 2,
  "kafkaStatus": {
    "isConnected": true,
    "circuitBreakerState": "CLOSED",
    "failureCount": 0
  },
  "pollingInterval": 5000,
  "lastPollTime": "2024-01-21T20:06:00.000Z",
  "eventsProcessed": 125,
  "eventsFailed": 3
}
```

---

## 🔬 Технические детали

### Алгоритм работы

1. **Polling**: Каждые 5 секунд опрашивает outbox таблицы всех сервисов
2. **Обработка**: Для каждого pending события:
   - Помечает как `processing`
   - Публикует в Kafka
   - При успехе помечает как `published`
   - При ошибке увеличивает счетчик попыток
3. **DLQ**: После 3 неудачных попыток отправляет событие в Dead Letter Queue
4. **Очистка**: Удаляет опубликованные события старше 30 дней

### Гарантии доставки

- **Атомарность**: События записываются в outbox в той же транзакции, что и бизнес-данные
- **Ретри**: Экспоненциальная задержка при ошибках
- **Circuit Breaker**: Защита от каскадных сбоев
- **DLQ**: Сохранение неудачных событий для последующего анализа

---

## 🛠️ Модули

### 1. `EventRelayApplication`

Основной класс приложения, координирующий работу всех компонентов:

```typescript
class EventRelayApplication {
  private readonly pollingIntervalMs: number;
  private readonly maxAttemptsPerEvent: number;
  private readonly services: MonitoredService[];
  
  constructor(config: EventRelayConfig);
  async start(): Promise<void>;
  private pollOutboxes(): Promise<void>;
  private processEvents(events: OutboxEvent[], service: MonitoredService): Promise<void>;
  private cleanupOldEvents(service: MonitoredService): Promise<void>;
}
```

### 2. `OutboxReaderRepository`

Работа с outbox таблицами различных сервисов:

```typescript
interface OutboxReaderRepository {
  getPendingEvents(limit?: number): Promise<OutboxEvent[]>;
  markAsProcessing(eventIds: string[]): Promise<void>;
  markAsPublished(eventIds: string[]): Promise<void>;
  markAsFailed(eventId: string, error: Error): Promise<void>;
}
```

### 3. `KafkaProducerService`

Публикация событий в Kafka с поддержкой Circuit Breaker:

```typescript
class KafkaProducerService {
  private readonly circuitBreaker: CircuitBreaker;
  private readonly producer: kafka.Producer;
  
  constructor(brokers: string[], clientId: string);
  async connect(): Promise<void>;
  async sendEvent(topic: string, event: PlatformEvent): Promise<void>;
  async disconnect(): Promise<void>;
}
```

### 4. `CircuitBreaker`

Реализация паттерна Circuit Breaker для защиты от каскадных сбоев:

```typescript
enum CircuitBreakerState {
  CLOSED = 'closed',
  OPEN = 'open',
  HALF_OPEN = 'half-open'
}

class CircuitBreaker {
  private state: CircuitBreakerState;
  private failureCount: number;
  private lastFailureTime: Date | null;
  
  execute<T>(operation: () => Promise<T>): Promise<T>;
  getState(): CircuitBreakerState;
  getFailureCount(): number;
}
```

---

## 🧪 Тестирование

### Unit тесты

```bash
npm test
```

### Интеграционные тесты

```bash
npm run test:integration
```

### Покрытие кода

```bash
npm run test:coverage
```

### E2E тесты

```bash
npm run test:e2e
```

---

## 🐳 Docker

### Сборка образа

```bash
docker build -t event-relay .
```

### Запуск контейнера

```bash
docker run -p 3006:3006 event-relay
```

### Health check

```bash
curl http://localhost:3006/health
```

---

## 📈 Производительность

### Рекомендации по масштабированию

1. **Горизонтальное масштабирование**:
   - Запуск нескольких инстансов event-relay
   - Использование `serviceName` в качестве ключа партиционирования Kafka

2. **Оптимизация БД**:
   - Индексы на `status` и `createdAt` в outbox таблицах
   - Регулярная очистка опубликованных событий

3. **Настройка Kafka**:
   - Увеличение количества партиций для топиков событий
   - Настройка retention policy

### Лимиты

- Максимальное количество событий за один опрос: 100
- Максимальное количество попыток отправки: 3
- Интервал опроса: 5 секунд
- Таймаут подключения к БД: 5 секунд

---

## 🔍 Troubleshooting

### Частые проблемы

#### 1. События не публикуются в Kafka

**Проверьте:**
- Подключение к Kafka брокерам
- Состояние Circuit Breaker
- Логи event-relay сервиса

#### 2. Высокая задержка обработки

**Решение:**
- Увеличьте количество партиций Kafka
- Оптимизируйте запросы к outbox таблицам
- Настройте индексы в БД

#### 3. Рост размера outbox таблиц

**Решение:**
- Проверьте работу очистки старых событий
- Увеличьте частоту опроса
- Проверьте подключение к Kafka

---

## 📊 Мониторинг

### Метрики Prometheus

- `event_relay_events_processed_total` - общее количество обработанных событий
- `event_relay_events_failed_total` - общее количество неудачных событий
- `event_relay_polling_duration_seconds` - длительность опроса outbox
- `event_relay_kafka_latency_seconds` - задержка при публикации в Kafka
- `event_relay_circuit_breaker_state` - состояние Circuit Breaker

### Логи

- Структурированное логирование в JSON формате
- Уровни: error, warn, info, debug
- Correlation ID для трассировки

---

## 🔄 Жизненный цикл события

```
┌─────────────┐
│    Created  │ ← Создается в том же транзакции, что и бизнес-объект
└────┬────────┘
     │
     ▼
┌─────────────┐
│   Pending   │ ← Состояние по умолчанию
└────┬────────┘
     │
     ▼
┌─────────────┐
│ Processing  │ ← Помечается при начале обработки
└────┬────────┘
     │
     ├─────────────► Success ──────────────┐
     │                                    ▼
     │                           ┌──────────────────┐
     │                           │   Published      │ ← Помечается при успешной отправке
     │                           └──────────────────┘
     │                                    │
     │                                    ▼
     │                           ┌──────────────────┐
     │                           │    Cleaned Up    │ ← Удаляется после 30 дней
     │                           └──────────────────┘
     │
     └─────────────► Failure ──────────────┐
                                         ▼
                               ┌──────────────────┐
                               │ Failed Attempts  │ ← Увеличивается счетчик попыток
                               └────────┬─────────┘
                                        │
                                        ▼
                              ┌──────────────────┐
                              │     DLQ          │ ← Отправляется в Dead Letter Queue
                              └──────────────────┘
```

---

## 📚 Дополнительные материалы

- [Transactional Outbox Pattern](https://microservices.io/patterns/data/transactional-outbox.html)
- [Circuit Breaker Pattern](https://microservices.io/patterns/reliability/circuit-breaker.html)
- [Kafka Best Practices](https://developer.confluent.io/learn-kafka/architecture/best-practices/)
- [Event-Driven Architecture](https://aws.amazon.com/architecture/event-driven/)

---

*Версия: 1.0.0*
*Обновлено: 2024-01-23*
*Автор: Platform Ecosystem Team*
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd 06_event-relay
npm install
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env файл при необходимости
```

### 3. Запуск в development режиме

```bash
npm run dev
```

### 4. Сборка и запуск

```bash
npm run build
npm start
```

### 5. Запуск через Docker

```bash
docker-compose up event-relay
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание   | Значение по умолчанию |
|------------|--------------|----------------------|
| `PORT`     | Порт сервиса | `3006` |
| `NODE_ENV` | Окружение | `development` |
| `KAFKA_BROKERS` | Kafka брокеры | `localhost:9092` |
| `KAFKA_CLIENT_ID` | Client ID для Kafka | `event-relay` |
| `AUTH_DB_HOST` | Хост БД auth-service | `localhost` |
| `AUTH_DB_PORT` | Порт БД auth-service | `5432` |
| `AUTH_DB_USERNAME` | Пользователь БД auth | `admin` |
| `AUTH_DB_PASSWORD` | Пароль БД auth | `secret` |
| `AUTH_DB_DATABASE` | Имя БД auth | `auth_db` |
| `USER_DB_HOST` | Хост БД user-service | `localhost` |
| `USER_DB_PORT` | Порт БД user-service | `5432` |
| `USER_DB_USERNAME` | Пользователь БД user | `admin` |
| `USER_DB_PASSWORD` | Пароль БД user | `secret` |
| `USER_DB_DATABASE` | Имя БД user | `user_db` |
| `POLLING_INTERVAL_MS` | Интервал опроса outbox | `5000` |
| `MAX_ATTEMPTS_PER_EVENT` | Макс. попыток отправки | `3` |

### Конфигурация Circuit Breaker

```typescript
{
  failureThreshold: 5,           // Количество ошибок до перехода в OPEN
  resetTimeout: 60000,           // Время в ms перед переходом в HALF_OPEN
  halfOpenMaxAttempts: 3         // Макс. попыток в HALF_OPEN
}
```

## 📡 API Endpoints

### Health Check

```http
GET /health
```

**Response:**
```json
{
  "status": "healthy",
  "timestamp": "2024-01-21T20:06:00.000Z",
  "services": {
    "auth-service": "connected",
    "user-service": "connected"
  },
  "kafka": {
    "isConnected": true,
    "circuitBreakerState": "CLOSED"
  }
}
```

### Статус сервиса

```http
GET /status
```

**Response:**
```json
{
  "isRunning": true,
  "monitoredServices": 2,
  "kafkaStatus": {
    "isConnected": true,
    "circuitBreakerState": "CLOSED",
    "failureCount": 0
  },
  "pollingInterval": 5000,
  "lastPollTime": "2024-01-21T20:06:00.000Z",
  "eventsProcessed": 125,
  "eventsFailed": 3
}
```

## 🔧 Технические детали

### Алгоритм работы

1. **Polling**: Каждые 5 секунд опрашивает outbox таблицы всех сервисов
2. **Обработка**: Для каждого pending события:
   - Помечает как `processing`
   - Публикует в Kafka
   - При успехе помечает как `published`
   - При ошибке увеличивает счетчик попыток
3. **DLQ**: После 3 неудачных попыток отправляет событие в Dead Letter Queue
4. **Очистка**: Удаляет опубликованные события старше 30 дней

### Гарантии доставки

- **Атомарность**: События записываются в outbox в той же транзакции, что и бизнес-данные
- **Ретри**: Экспоненциальная задержка при ошибках
- **Circuit Breaker**: Защита от каскадных сбоев
- **DLQ**: Сохранение неудачных событий для последующего анализа

### Мониторинг

- **Метрики Prometheus**:
  - `event_relay_events_processed_total`
  - `event_relay_events_failed_total`
  - `event_relay_polling_duration_seconds`
  - `event_relay_kafka_latency_seconds`

- **Логи**:
  - Структурированное логирование в JSON формате
  - Уровни: error, warn, info, debug
  - Correlation ID для трассировки

## 🧪 Тестирование

### Unit тесты

```bash
npm test
```

### Интеграционные тесты

```bash
npm run test:integration
```

### Покрытие кода

```bash
npm run test:coverage
```

## 🐳 Docker

### Сборка образа

```bash
docker build -t event-relay .
```

### Запуск контейнера

```bash
docker run -p 3006:3006 event-relay
```

### Health check

```bash
curl http://localhost:3006/health
```

## 📈 Производительность

### Рекомендации по масштабированию

1. **Горизонтальное масштабирование**:
   - Запуск нескольких инстансов event-relay
   - Использование `serviceName` в качестве ключа партиционирования Kafka

2. **Оптимизация БД**:
   - Индексы на `status` и `createdAt` в outbox таблицах
   - Регулярная очистка опубликованных событий

3. **Настройка Kafka**:
   - Увеличение количества партиций для топиков событий
   - Настройка retention policy

### Лимиты

- Максимальное количество событий за один опрос: 100
- Максимальное количество попыток отправки: 3
- Интервал опроса: 5 секунд
- Таймаут подключения к БД: 5 секунд

## 🔍 Troubleshooting

### Частые проблемы

#### 1. События не публикуются в Kafka

**Проверьте:**
- Подключение к Kafka брокерам
- Состояние Circuit Breaker
- Логи event-relay сервиса

#### 2. Высокая задержка обработки

**Решение:**
- Увеличьте количество партиций Kafka
- Оптимизируйте запросы к outbox таблицам
- Настройте индексы в БД

#### 3. Рост размера outbox таблиц

**Решение:**
- Проверьте работу очистки старых событий
- Увеличьте частоту опроса
- Проверьте подключение к Kafka

### Логи

```bash
# Просмотр логов event-relay
docker logs event-relay

# Просмотр логов с фильтрацией
docker logs event-relay --tail 100 | grep -E "(ERROR|WARN|Published|Failed)"

# Мониторинг в реальном времени
docker logs event-relay -f
```

## 📚 Дополнительные материалы

- [Transactional Outbox Pattern](https://microservices.io/patterns/data/transactional-outbox.html)
- [Circuit Breaker Pattern](https://microservices.io/patterns/reliability/circuit-breaker.html)
- [Kafka Best Practices](https://developer.confluent.io/learn-kafka/architecture/best-practices/)

## 👥 Разработка

### Структура проекта

```
06_event-relay/
├── src/
│   ├── main.ts                    # Точка входа
│   ├── application/               # Бизнес-логика
│   │   └── EventRelayApplication.ts
│   ├── infrastructure/            # Внешние зависимости
│   │   ├── db/                    # Работа с БД
│   │   │   ├── DatabaseConfig.ts
│   │   │   └── OutboxReaderRepository.ts
│   │   └── messaging/            # Работа с Kafka
│   │       ├── KafkaProducerService.ts
│   │       └── CircuitBreaker.ts
│   └── presentation/             # API и health checks
├── tests/                        # Тесты
├── Dockerfile                    # Docker конфигурация
├── docker-compose.yml           # Локальный запуск
└── package.json                 # Зависимости
```

### Code Style

- TypeScript с strict mode
- ESLint + Prettier
- Jest для тестирования
- Структурированное логирование

---

*Версия: 1.0.0*
*Обновлено: 2024-01-21*